/// <reference types="expo/types" />

// Expo `expo/types` `process.env.EXPO_PUBLIC_*` uchun tiplarni beradi.
// Bu fayl `expo-env.d.ts` (avto-generatsiya, gitignore) o'rnini bosadi —
// shu sabab standalone `tsc` ham xatosiz ishlaydi.
